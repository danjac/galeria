{{#link-to 'image-detail' image class="thumbnail" title=image.title}}
    <img src={{image.thumbnail.url}} 
         height={{image.thumbnail.height}} 
         width={{image.thumbnail.width}} 
         alt={{image.title}} />
{{/link-to}}
